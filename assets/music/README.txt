The background music currently streams from a Pixabay CDN URL (see the
<audio id="bgm"> tag in index.html), by original design.

To use your own track instead:
1. Save it here as forever.mp3.
2. In index.html, change the <source src="..."> inside <audio id="bgm">
   to src="assets/music/forever.mp3".
